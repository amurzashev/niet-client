const intialState = {
  clients: [
    // { iin: '', privilege: false }
  ],
  cars: [
    // 'license plate num'
  ],
};

export default (state = intialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
