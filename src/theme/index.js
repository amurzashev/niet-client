const theme = {};

theme.colors = {
  white: '#fff',
  primary: '#00a6ed',
  lightGray: '#eee',
  darkGray: '#e2e2e2',
  error: 'red',
};

theme.breakpoints = {
  mobile: '@media only screen and (max-width: 920px)',
};

theme.media = {
  mobile: '(max-width: 920px)',
};

export default theme;
